# Blog/sermon-notes index. The /blog/ pages are generated by build_blog()
# from content/blog/*.md, so this YAML only exists to put "Blog" into the
# nav. No blocks needed.
meta:
  title: "Sermon Notes — The River Church"
  description: "Weekly sermon recaps from The River Church in Post Falls, Idaho."
  nav_label: Blog
  show_in_nav: true
  nav_order: 30
  slug: blog
  no_html: true   # build_blog handles /blog/ rendering

blocks: []
